apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: antivirus-immunity
  labels:
    app.kubernetes.io/name: antivirus-immunity
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: antivirus-immunity
  endpoints:
    - port: metrics
      path: /metrics
      interval: 30s
